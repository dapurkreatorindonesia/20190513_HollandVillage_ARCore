<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.dapurkreatorindonesia.hollandvillage">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Permission AR -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera.ar" android:required="true"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key"/>
        <meta-data android:name="com.google.ar.core" android:value="required"/>

        <activity
                android:name=".SplashScreenActivity"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".onboard.OnboardActivity">
        </activity>

        <activity
                android:name=".MainActivity">
        </activity>
        <activity
                android:name=".AboutActivity">
        </activity>

        <!--Room AR-->
        <activity
                android:name=".room.RoomActivity">
        </activity>
        <activity
                android:name=".room.B3Activity">
        </activity>
        <activity
                android:name=".room.B5Activity">
        </activity>
        <activity
                android:name=".room.C12Activity">
        </activity>

        <!--Facilities AR-->
        <activity
                android:name=".facilities.FacilityActivity">
        </activity>
        <activity
                android:name=".facilities.BasketActivity">
        </activity>
        <activity
                android:name=".facilities.FutsalActivity">
        </activity>
        <activity
                android:name=".facilities.VollyActivity">
        </activity>

        <activity
                android:name=".maps.MapsActivity">
        </activity>

        <activity
                android:name=".facilities.HowtoUseFacilitiesActivity">
        </activity>

        <activity
                android:name=".room.HowToUseActivity">
        </activity>

    </application>

</manifest>